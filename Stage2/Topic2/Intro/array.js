


var list = [15, 20, 25, 30, 35, 40, 45, 25, 50];
// var list = ["+", 994, 10, 310, 38, 97];
var fruits = ["Apple", "Banana", "Cherry", "Date", "Elderberry"];


// list.push(55); // Add 55 to the end of the list
// list.push("Banana"); // Add "Banana" to the end of the list
// var silinenEl =  list.pop(); // Remove the last element from the list
// list.pop(); // Remove the last element from the list
// list.shift(); // Remove the first element from the list
// list.unshift(5); // Add 5 to the beginning of the list

// var result = list.includes(125);
// var result = list.indexOf(25);
// var result = list.lastIndexOf(25);
// var result = list.join(""); // Join the elements of the list into a string
// list.reverse() // list.sort() // Sort the list in ascending order
// fruits.sort() // Sort the fruits alphabetically

// var result = list.slice(0, 5); // Get a subarray from index 0 to index 5 (exclusive)
// var result = list.splice(0, 0,100,101); // Remove the first 5 elements from the list
// var result = list.splice(2, null,100,101); // Add 100 and 101 at the beginning of the list
// var result = list.splice(1, 1); // Add 100 and 101 at the beginning of the list
// var result = list.splice(1, 1, 100, 101); // Add 100 and 101 at the beginning of the list

// var newList = list.concat(fruits); // Concatenate fruits to the list

console.log("Updated list:", list);
console.log("Result of includes check:", result);

